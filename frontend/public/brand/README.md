This folder is for public brand assets (logos, icons, etc.).

In Next.js, files in `frontend/public/` are served from the site root.

Example:
- Put `logo.svg` here
- Use it in code as `/brand/logo.svg`

